{{!-- <h1>Product list</h1>
<div class="product-list">

  {{#each products}}
    <div class="product-card">
      <h2> {{this.title}} </h2>
      <p>{{this.description}}</p>
      <p>{{this.price}}</p>
    </div>

  {{/each}}
</div> --}}